{
  "name": "coursepass-v4-player",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CoursePass V4 activity player — plays a unit's mixed activity set from companion-api with native MathML rendering.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test"
  }
}
